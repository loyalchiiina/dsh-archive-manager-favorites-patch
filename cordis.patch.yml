# 保留官方工作区 UI，仅替换归档需要的后端服务。
# 自定义侧栏通过优先级覆盖插槽；发布后的安装命令：
#   dsh plugin --profile web add dsh-archive-manager-plus
- id: workspace
  disabled: true
- id: session-projection-cache
  disabled: true
- id: ui-workspace
  disabled: false
- insert:
    - id: workspace-archive-manager
      name: 'dsh-archive-manager-plus/workspace'
    - id: session-projection-cache-archive-manager
      name: 'dsh-archive-manager-plus/projcache'
      config:
        writeEveryEvents: 200
        writeIntervalMs: 5000
    - id: ui-workspace-archive-manager
      name: 'dsh-archive-manager-plus'
